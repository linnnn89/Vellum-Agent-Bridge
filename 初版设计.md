# Vellum → Semantic UI Spec → WPF/XAML Bridge

> **文档角色**：初版 MVP 设计简报。正文是当时的实现指令，不是逐字照抄的现行规范。
> **最后核对**：2026-09-06
>
> 状态标记：
>
> - **已完成**：已落地，并用测试 / `dotnet build` / canary 验证
> - **已废弃**：原文不再作为实现依据；以「取代」说明为准
> - **仍有效**：约束继续遵守
> - **部分完成**：主路径完成，限制写在条文里

## 实现状态总表

| 章节 | 状态 | 备注 |
| --- | --- | --- |
| 开篇目标与原则 | 已完成 / 仍有效 | 流水线已打通；原则仍适用 |
| 0 检查工作区 | 已完成 | 独立目录 `tools/vellum-wpf-bridge/`，未改 Vellum 核心 |
| 1 `.vellum` 适配层 | 已完成（已强化） | `VellumDocumentAdapter` + 原生严格校验 `validator.py`，杜绝静默容错 |
| 2 Semantic UI Spec | 已完成（已强化） | `schemas/ui-spec.schema.json`；自包含 `resources`，解耦 `.vellum` 源文件 |
| 3 映射规则 | 已完成 | Button / Input / Card / Text 启发式已落地并收紧 |
| 4 Layout 转换 | **已完成（已修正）** | 遵守现行 Layout Contract；Border Padding 包装器，Canvas 尺寸保留与告警 |
| 5 Appearance | 已完成 | Shadow 走 warning，未生成 `DropShadowEffect` |
| 6 颜色 | 已完成 | Token 直接写入 `UiSpec.resources`，支持独立生成 XAML |
| 7 WPF Generator | 已完成（已修正） | `Border Padding` 包装；TextBox 禁止填入 placeholder；精简按需 `x:Name` |
| 8 MVVM 边界 | 已完成 / 仍有效 | 只在名称足够明显时生成 `Command`，不生成 ViewModel |
| 9 Name 推断 | 已完成 | 过滤 `Rectangle 43` 等空名，按需生成确定性 `x:Name` |
| 10 CLI | 已完成（已强化） | `pyproject.toml` 标准打包，免环境变量依赖；提供 5 大核心子命令 |
| 11 Conversion Report | 已完成 | 已扩展 promoted / absorbed / diagnostics / normalized |
| 12 优雅降级 | 已完成 | path/ellipse fallback；Canvas 丢失响应式告警 |
| 13 Source metadata | 已完成 | Spec `source` + XAML `<!-- Vellum: id / name -->` |
| 14 禁止默认绝对定位 | 已完成 | 仅 freeform 使用 Canvas |
| 15 不要过度推断 | **已废弃 / 已规范** | 严格基于设计约束；未知 → Canvas |
| 16 AI 接口 | 已完成（已强化） | `SafeAgentRefiner` + `ui-spec-patch.schema.json`（语义白名单+黑名单+SHA-256） |
| 17 测试 | 已完成（47 单测） | 含 260\|*\|260 Canary、严密校验、Refiner 拦截与独立 generate 测试 |
| 18 WPF Demo | 已完成 | `samples/generated-wpf-demo/`，`dotnet build` 0 错误 0 警告 |
| 19 Demo UI | 已完成 | `samples/tabletop-chat.vellum`，可通过 Vellum 与严格 Python 校验 |
| 20 README | 已完成 | 中英文对标高 Star 规范对公 README，内部硬约束收口至 `docs/` |
| 21 技术选择 | 已完成 | Python 3 标准库零外部依赖；setuptools 标准打包 |
| 22 代码结构 | 已完成 | 增加 `validator.py` / `refiner.py` / `pyproject.toml` |
| 23 不要做的事 | 仍有效 | 坚决不接在线 LLM API，不生业务 ViewModel，不跨 Avalonia/React |
| 24 成功标准 | 已完成 | 全量验收标准与 4 阶段目标 100% 达成 |
| 25 真实验证 | 已完成 | 47 项单测全绿，`dotnet build` 0 警告 0 错误 |
| 26 汇报格式 | 已完成 | 已提供标准交付总结 |
| 27 Vibe Coding 目标 | 仍有效 | 可读 / 可改优先于图形完备 |

## 现行 Layout Contract（取代 §4 / §15 中已废弃条款）

Sizing、Anchoring、Flow 三个维度分开，禁止用图层名猜测布局。

```text
widthMode / heightMode:  fixed | fill
constraintH / constraintV: 源字段原样保留（left/right/center/stretch/scale）
designWidth / designHeight: 设计时用值，只作提示

LayoutContract 禁止 hug 推断。
只有 constraint == stretch → fill；其余默认 fixed。
```

生成器硬约束：

```text
AutoLayout 且主轴无 fill  → StackPanel + 非末子项 trailing Margin（gap）
AutoLayout 且主轴有 fill  → Grid；逻辑列/行保留在 Spec，XAML 用 spacer track 展开 gap
Freeform                  → Canvas + Left/Top + ABSOLUTE_LAYOUT_NOT_RESPONSIVE
fill / * 轨道内的子节点   → 禁止再写死该轴 Width/Height
根内容面板                → 禁止复制 Window 的 Width/Height
同一轴多个 stretch        → 等权 * + LAYOUT_MULTI_STRETCH_NORMALIZED
```

心跳 canary（内容宽，不含窗口 chrome）：

```text
260 | * | 260
1280 → 260 | 760  | 260
1600 → 260 | 1080 | 260
delta →  0 | +320 | 0
```

实现与测试入口：

```text
tools/vellum-wpf-bridge/
python tests/run_tests.py
```

---

以下为初版实现指令原文。每节标题下的状态说明优先于正文。

---

# 开篇：目标与原则

> 状态：**已完成**（流水线可运行）。原则 **仍有效**。
> 文首「你现在要在现有代码环境中完成……」以及文末「现在开始检查代码库并直接实现」是一次性开工指令，**已完成，不再作为待办**。

目标不是做一个“完美的 Figma-to-code 转换器”，而是验证以下 Vibe Coding 工作流是否可行：

```text
Vellum 设计稿
    ↓
.vellum JSON
    ↓
解析器
    ↓
语义化 UI Spec
    ↓
WPF/XAML Generator
    ↓
可运行的 WPF UI
```

核心原则：

> Vellum 负责视觉设计和布局表达；
> 中间 Semantic UI Spec 负责把设计语义和具体 UI 框架解耦；
> 最终生成器负责根据 UI Spec 输出相对干净、可维护的 WPF/XAML。

本次不要追求完整 Vellum 功能支持。

目标是完成一个真正可运行、可验证、方便后续让 AI Agent 继续扩展的 MVP。

---

# 0. 首先检查工作区

> 状态：**已完成**。Bridge 在 `tools/vellum-wpf-bridge/`，未修改 Vellum 编辑器核心代码。

开始编码前：

1. 检查当前工作区是否已经存在 Vellum 源码。
2. 如果存在，阅读：

   * README.md
   * ARCHITECTURE.md
   * src/document.js
   * 与 `.vellum` 导入/导出、document model、frame、text、component、auto-layout 有关的代码
3. 如果不存在 Vellum 源码，但当前环境允许访问 Git：

   * 使用官方仓库：
     `wieslawsoltes/Vellum`
4. 不要修改 Vellum 原始编辑器的核心代码，除非确实需要一个非常小的兼容性改动。
5. Bridge 尽量做成独立目录或独立工具。

建议目录：

```text
tools/
└── vellum-wpf-bridge/
    ├── README.md
    ├── samples/
    ├── schemas/
    ├── src/
    ├── tests/
    └── output/
```

如果当前项目已有更合适的目录结构，可以调整。

---

# 1. 先理解 `.vellum` 的真实数据结构

> 状态：**已完成**。字段以 `src/document.js` / `src/app.js` 为准（`layout`/`gap`/`padding`/`layoutAlign`/`constraintH`/`constraintV`）。WPF Generator 不读原始 `.vellum`。

禁止根据猜测直接设计 parser。

请从 Vellum 源码和实际 `.vellum` 文件中确认：

* document 根结构
* pages
* nodes / layers
* node id
* node type
* parent-child hierarchy
* x/y/width/height
* rotation
* fills
* strokes
* opacity
* corner radius
* text
* font size
* font weight
* text alignment
* frame
* group
* auto-layout
* gap
* padding
* constraints
* component / instance
* visibility
* clipping

如果字段名称与这里不一致，以源码为准。

请在代码中保持清晰的输入适配层：

```text
Vellum raw JSON
        ↓
VellumDocumentAdapter
        ↓
normalized nodes
```

不要让 WPF Generator 直接读取原始 `.vellum` JSON。

---

# 2. 设计 Semantic UI Spec

> 状态：**已完成**。文件：`tools/vellum-wpf-bridge/schemas/ui-spec.schema.json`、`models.py`。
>
> **已废弃（示例结构）**：下面 JSON 把窗口子节点放在顶层 `children`，并用 `grow: 1` 表示弹性。现行 Spec 使用 `root` 树，弹性用 `widthMode`/`heightMode` + `constraintH`/`constraintV`，不再用 `grow` 作为布局契约。

这是整个项目最重要的部分。

设计一个简单、清楚、易于 AI 修改的 JSON schema。

建议文件：

```text
schemas/ui-spec.schema.json
```

示例结构可以类似：

```json
{
  "version": "0.1",
  "name": "MainWindow",
  "type": "window",
  "width": 1440,
  "height": 900,
  "theme": "dark",
  "children": [
    {
      "id": "sidebar",
      "name": "CharacterSidebar",
      "type": "panel",
      "layout": {
        "direction": "vertical",
        "width": 260,
        "gap": 8,
        "padding": 12
      },
      "children": []
    },
    {
      "id": "workspace",
      "name": "ChatWorkspace",
      "type": "panel",
      "layout": {
        "grow": 1,
        "direction": "vertical"
      },
      "children": []
    }
  ]
}
```

不要机械复制 Vellum 的所有字段。

Semantic UI Spec 应该体现“UI 语义”，而不是纯绘图场景。

至少支持这些语义节点：

```text
window
panel
frame
stack
grid
text
button
input
image
separator
card
badge
icon
unknown
```

不确定的节点统一降级为：

```json
{
  "type": "unknown",
  "sourceType": "..."
}
```

而不是报错终止整个转换。

---

# 3. 第一版 Vellum → UI Spec 映射规则

> 状态：**已完成**。Button / Input 启发式已收紧（名称含 panel/sidebar/header/card 等不升成 button）。不确定节点降为 `unknown`，不中断转换。

先实现常见场景。

## Frame

如果 Vellum Frame：

* 有 auto-layout：
  → 转换成 stack/panel
* 没有 auto-layout：
  → ~~转换成 frame / grid-like container~~ **已废弃** → 现行：`absolute` + Canvas，并记 `ABSOLUTE_LAYOUT_NOT_RESPONSIVE`

例如：

```text
horizontal auto-layout
→ horizontal StackPanel / Grid-like panel

vertical auto-layout
→ vertical StackPanel
```

---

## Text

转换：

```text
Vellum text
→ UI Spec text
```

保留：

* content
* font family
* font size
* font weight
* italic
* text alignment
* foreground/fill
* opacity

---

## Rectangle

根据上下文判断。

普通矩形：

```text
rectangle
→ frame / panel
```

如果包含：

* corner radius
* fill
* shadow
* child content

则优先语义化为：

```text
card
```

不要试图使用复杂 AI 推理。

第一版可以使用简单 deterministic heuristic。

---

## Button heuristic

如果一个 Frame / Rectangle：

* 有背景色
* 有圆角
* 内部只有一个短文本节点
* 高度大致在常见按钮范围

则：

```text
→ button
```

例如内部文字：

```text
Save
Send
Cancel
设置
发送
确认
```

不要求识别所有按钮。

---

## Input heuristic

如果：

* 横向长矩形
* 有边框或背景
* 内部有 placeholder-like text

可以转换成：

```text
input
```

如果不能稳定识别，保留为 frame。

---

# 4. Layout 转换规则

> 状态：**部分已废弃，已被文首「现行 Layout Contract」取代**。
> 保留的意图仍有效：优先布局关系，而不是像素复制。
>
> **已废弃**：
>
> - Auto Layout 无条件 → StackPanel
> - 用图层名（chat / workspace）决定谁拿 `*`
> - Grid gap 一律写成 child Margin（会从固定列宽里偷空间）
> - 在 `*` 列/行里再写死子节点 Width/Height
>
> **现行**：主轴有 `fill` 必须 Grid + star；无 fill 才 StackPanel；Grid gap 用 spacer track。

这是 MVP 成败的重点。

优先保留布局关系，而不是追求像素级复制。

## Auto Layout

> 下列「一律 StackPanel」**已废弃**。仅当主轴没有任何 `widthMode`/`heightMode = fill` 的子节点时，才生成 StackPanel。

Vellum vertical Auto Layout：

```text
→ WPF StackPanel Orientation="Vertical"   # 仅主轴无 fill
→ 主轴有 fill 时：Grid + RowDefinitions（含 spacer）
```

Vellum horizontal Auto Layout：

```text
→ WPF StackPanel Orientation="Horizontal"  # 仅主轴无 fill
→ 主轴有 fill 时：Grid + ColumnDefinitions（含 spacer）
```

如果存在明显 grow/stretch 行为，则允许使用 Grid。

---

## 固定宽度

例如：

```text
sidebar width = 280
```

生成：

```xml
<ColumnDefinition Width="280"/>
```

---

## Grow / Fill

> **已废弃** `grow = 1` 作为源语义。Vellum 没有 hug/grow 元数据。现行投影：`constraintH/V == stretch` → `widthMode/heightMode = fill` → Grid `*`。

转换：

```text
~~grow = 1~~  constraint stretch → fill → *
```

例如：

```xml
<ColumnDefinition Width="*"/>
```

---

## Padding

优先：

```xml
Padding="12"
```

或合理的 Margin 包装。

---

## Gap

WPF 没有所有容器统一的 gap。尽量不要为了 Gap 引入复杂第三方库。

> **已废弃**「所有 gap 都用 child Margin」。
>
> **现行**：
>
> - StackPanel gap → 非最后一个 child 的 trailing Margin（加在子项外侧，不偷 Width）
> - Grid gap → spacer `ColumnDefinition` / `RowDefinition`（Spec 仍只存逻辑列 `[260, "*", 260]`）

---

# 5. Appearance 映射

> 状态：**已完成**。Fill / Stroke / CornerRadius / Opacity / Foreground 已映射。
> Shadow：**未**生成 `DropShadowEffect`，记 `SHADOW_IGNORED`。Gradient 记 `GRADIENT_FALLBACK` 并使用 solid fill。

至少支持：

## Fill

```text
solid fill
→ Background
```

## Stroke

```text
stroke
→ BorderBrush + BorderThickness
```

## Corner radius

```text
→ CornerRadius
```

## Opacity

```text
→ Opacity
```

## Text color

```text
→ Foreground
```

## Shadow

如果容易实现：

```text
→ DropShadowEffect
```

否则第一版可以忽略，并产生 warning。

---

# 6. 颜色处理

> 状态：**已完成**。Vellum `tokens.colors` 与重复 solid 色写入 `Resources.xaml`。
> Token 没有节点级 `tokenId`，按精确色值匹配，report 标 `TOKEN_INFERRED_BY_VALUE`。

不要在生成的 XAML 中到处重复：

```xml
Background="#1E1E1E"
Foreground="#FFFFFF"
```

如果同一种颜色重复出现较多，应自动生成资源，例如：

```xml
<SolidColorBrush x:Key="Color.Background.Primary"
                 Color="#1E1E1E"/>

<SolidColorBrush x:Key="Color.Text.Primary"
                 Color="#FFFFFF"/>
```

然后：

```xml
Background="{StaticResource Color.Background.Primary}"
```

第一版无需实现复杂设计 token 推断。

简单按重复颜色去重即可。

---

# 7. WPF Generator

> 状态：**已完成**。输出 `MainWindow.xaml`、`Resources.xaml`、`App.xaml`。重点仍是视觉结构，不是业务代码。

实现：

```text
UI Spec
    ↓
WpfGenerator
    ↓
MainWindow.xaml
```

必要时同时生成：

```text
App.xaml
Resources.xaml
MainWindow.xaml
```

本次重点是：

```text
视觉结构
```

而不是业务代码。

---

# 8. MVVM 边界

> 状态：**已完成** / **仍有效**。`SendButton` → `Command="{Binding SendCommand}"`。不生成 ViewModel。

不要尝试自动生成完整业务逻辑。

如果识别到：

```text
button
```

可以输出：

```xml
<Button Content="Send"
        Command="{Binding SendCommand}" />
```

但是：

只有节点名称或语义足够明显时才生成 Command。

例如：

```text
SendButton
→ SendCommand

SaveButton
→ SaveCommand
```

否则：

```xml
<Button Content="..." />
```

不要编造大量 ViewModel。

---

# 9. Name 推断

> 状态：**已完成**。`to_pascal_case()` 丢弃 `Rectangle 43` / `Frame 12` 等空名。

如果 Vellum layer 有名字：

```text
Character Sidebar
Chat Workspace
Send Button
Settings Panel
```

转换为合法 PascalCase：

```text
CharacterSidebar
ChatWorkspace
SendButton
SettingsPanel
```

需要时生成：

```xml
x:Name="SendButton"
```

无意义名字例如：

```text
Rectangle 43
Frame 129
Group 12
```

不要生成垃圾 x:Name。

---

# 10. CLI

> 状态：**已完成**。现行命令（从 `tools/vellum-wpf-bridge` 运行）：
>
> ```bash
> $env:PYTHONPATH="src"; python -m vellum_wpf_bridge convert samples/tabletop-chat.vellum -o output
> $env:PYTHONPATH="src"; python -m vellum_wpf_bridge convert samples/tabletop-chat.vellum -o output --spec-only
> ```
>
> **已废弃** 示例路径 `samples/main-window.vellum`（实际 sample 为 `tabletop-chat.vellum`）。未做 `pyproject.toml` 安装，故需要 `PYTHONPATH=src`。

实现一个简单 CLI。

目标：

```bash
vellum-wpf convert input.vellum
```

例如：

```bash
python -m vellum_wpf_bridge convert samples/main-window.vellum
```

输出：

```text
output/
├── ui-spec.json
├── MainWindow.xaml
├── Resources.xaml
└── conversion-report.json
```

最好支持：

```bash
--spec-only
```

例如：

```bash
python -m vellum_wpf_bridge convert input.vellum --spec-only
```

只生成 UI Spec。

---

# 11. Conversion Report

> 状态：**已完成**。现行 report 在原文字段之外增加 `nodesAbsorbed`、`promoted`、`absorbed`、`normalized`、`diagnostics`（含 `GRADIENT_FALLBACK`、`SHADOW_IGNORED`、`CLIP_IGNORED`、`ABSOLUTE_LAYOUT_NOT_RESPONSIVE`、`LAYOUT_MULTI_STRETCH_NORMALIZED`、`TOKEN_INFERRED_BY_VALUE`）。

生成：

```text
conversion-report.json
```

内容包括：

```json
{
  "nodesTotal": 120,
  "nodesConverted": 113,
  "nodesFallback": 7,
  "unsupported": [
    {
      "id": "...",
      "type": "...",
      "reason": "..."
    }
  ],
  "warnings": []
}
```

这样未来 Agent 可以知道：

> 哪些部分成功转换，哪些地方需要人工或 AI 修正。

---

# 12. 非支持功能必须优雅降级

> 状态：**已完成**。不 throw；path/ellipse → `unknown` + unsupported；gradient/shadow/clip → diagnostic。component variants / animation / rich text 仍不支持。

第一版不要求支持：

* arbitrary Bézier
* complex masks
* Boolean vector operation
* gradients
* rich text spans
* prototype links
* advanced blend modes
* component variants
* complex nested constraints
* arbitrary SVG editing
* animation

遇到这些情况：

不要：

```text
throw fatal error
```

而应该：

```text
warning + fallback
```

例如：

```json
{
  "warning": "Gradient fill is not supported; first solid stop used."
}
```

---

# 13. 保留 Source Metadata

> 状态：**已完成**。UI Spec 含 `source.format/nodeId/nodeName/sourceType`；XAML 含 `<!-- Vellum: {nodeId} / {nodeName} -->`。

这是以后 Agent 修改 UI 很重要的基础。

UI Spec 中建议保留：

```json
{
  "source": {
    "format": "vellum",
    "nodeId": "abc123",
    "nodeName": "Send Button"
  }
}
```

最终 XAML 中如果方便，可以生成：

```xml
<!-- Vellum: abc123 / Send Button -->
```

这样未来才能实现：

```text
Vellum node
↔ UI Spec node
↔ XAML element
```

---

# 14. 一个重要设计要求：不要直接把所有节点绝对定位

> 状态：**已完成**。Auto Layout 走 StackPanel/Grid；仅无 auto-layout 的 freeform 使用 Canvas。禁止在 `*` 轨道里再写死像素尺寸。

这是本项目与普通 Figma-to-code 工具最关键的区别。

禁止默认生成：

```xml
<Canvas>
    <Button Canvas.Left="821"
            Canvas.Top="613"
            Width="92"
            Height="40"/>
</Canvas>
```

除非原始 Vellum Frame 明显就是自由画布布局。

优先把视觉结构推断成：

```text
Grid
StackPanel
Border
DockPanel
```

目标是：

> 尽量恢复布局意图，而不是机械复制坐标。

---

# 15. 但也不要过度“智能推断”

> 状态：**仍有效**（deterministic、禁止 LLM 布局推断）。其中「明显横向区域 → Grid」**已废弃**，见上。

不要设计：

```text
LLM layout inference engine
AI semantic classifier
complex constraint solver
```

第一版尽可能 deterministic。

例如：

```text
AutoLayout 无 fill → StackPanel
AutoLayout 有 fill → Grid star
未知 / 无 Auto Layout → Canvas fallback
```

> **已废弃**：
>
> ```text
> AutoLayout → StackPanel          # 无条件映射
> 明显横向区域 → Grid columns      # 空间/名字猜测
> 普通容器 → Grid
> ```
>
> 不得用 `"chat" in name` 一类子串决定 stretch。DockPanel 不是 v2 必选路径，Grid 已覆盖三栏与 Header/Body/Footer。

---

# 16. 为以后 AI Agent 留接口

> 状态：**已完成**。`UiSpecRefiner.refine()` 为空实现，CLI 会调用但不接 LLM。**仍有效**：本阶段不要接模型。

架构中预留：

```text
post-processors
```

例如：

```text
UI Spec
    ↓
RuleBasedNormalizer
    ↓
OptionalAgentRefiner
    ↓
WpfGenerator
```

但是本次不要真的接 LLM API。

定义接口即可，例如：

```python
class UiSpecRefiner:
    def refine(self, spec: UiSpec) -> UiSpec:
        return spec
```

未来我可以让 Codex / OpenAI Agent：

```text
读取 UI Spec
↓
理解界面语义
↓
重新组织布局
↓
再生成 XAML
```

---

# 17. 测试

> 状态：**已完成**，并已超出原文清单。现行入口：`python tests/run_tests.py`（31 passed，含 Layout Contract TDD 与 WPF `ActualWidth` canary）。
> 另有 `scripts/validate-vellum.mjs` 调用官方 `DocumentModel.parse()`。

至少建立这些测试。

## Parser test

确认 `.vellum`：

```text
→ normalized document
```

不会丢失基本 hierarchy。

---

## Mapping test

至少测试：

```text
Frame
Text
Rectangle
Auto Layout
Padding
Gap
Fill
Stroke
Corner radius
```

---

## Generator test

测试：

```text
UI Spec
→ valid XML
```

---

## Golden test

准备一个小型 sample：

```text
sidebar
workspace
bottom input
send button
```

转换以后保存：

```text
expected/MainWindow.xaml
```

做 snapshot/golden comparison。

---

# 18. 如果环境支持 .NET

> 状态：**已完成**。`samples/generated-wpf-demo/`，目标 `net10.0-windows`，`dotnet build` 0 错误 0 警告。

如果机器已经安装：

```text
dotnet
```

则创建一个最小 WPF Demo：

```text
samples/generated-wpf-demo/
```

目标：

```bash
dotnet build
```

成功。

如果当前系统不是 Windows 或无法构建 WPF：

不要为此阻塞整个任务。

至少：

* 确认 XAML XML 合法
* 输出完整生成文件
* 在 README 中记录未实际 WPF build

---

# 19. 第一个 Demo UI

> 状态：**已完成**。`samples/tabletop-chat.vellum` 含 `rotation`/`opacity` 等 parse 必填项，可通过 Vellum `DocumentModel.parse()`。左 260 / 中 `*` / 右 260，底栏 Input + Send。

为了验证价值，制作一个非常简单但真实的桌面聊天/跑团类 UI：

```text
┌──────────────────────────────────────────────────┐
│               App Header                         │
├─────────────┬────────────────────────┬───────────┤
│ Characters  │                        │ Inspector │
│             │      Chat Area         │           │
│ Player A    │                        │ Model     │
│ Player B    │                        │ Settings  │
│ Player C    │                        │           │
│             │                        │           │
├─────────────┴────────────────────────┴───────────┤
│ Message input                         Send       │
└──────────────────────────────────────────────────┘
```

要求：

* 左侧固定宽度
* 中间自适应
* 右侧固定宽度
* 中央区域 vertical layout
* 底部输入栏
* Send button
* dark theme
* card-like panels
* rounded corners

最好直接在 Vellum 中创建或修改一个对应 sample，再导出 `.vellum`。

如果自动创建 Vellum sample 很困难，可以手工构造最小合法 `.vellum`，但必须依据真实 schema。

---

# 20. README

> 状态：**已完成**。`tools/vellum-wpf-bridge/README.md`。

写一个简洁但完整的：

```text
tools/vellum-wpf-bridge/README.md
```

包含：

## What it does

解释：

```text
Vellum
→ Semantic UI Spec
→ WPF XAML
```

## Why Semantic UI Spec

解释：

不要：

```text
Vellum → XAML
```

直接绑定。

而使用：

```text
Vellum
→ framework-neutral spec
→ WPF
```

以后才能支持：

```text
React
Avalonia
Flutter
HTML
```

---

## Usage

给出实际可复制命令。

---

## Supported mappings

列出：

```text
supported
partial
unsupported
```

---

## Architecture

画简单 ASCII：

```text
.vellum
   │
   ▼
VellumAdapter
   │
   ▼
NormalizedDocument
   │
   ▼
SemanticMapper
   │
   ▼
UI Spec
   │
   ├────────► future AI Refiner
   │
   ▼
WpfGenerator
   │
   ▼
XAML
```

---

# 21. 技术选择

> 状态：**已完成**。Python 3 + 标准库 + `argparse` + `unittest`。允许的 pydantic / jsonschema / pytest **未引入**（非必须）。

优先：

```text
Python 3
```

原因：

* JSON parser 简单
* schema validation 简单
* CLI 简单
* Agent 容易继续修改
* 与 Vellum 本身解耦

允许依赖：

```text
pydantic
jsonschema
pytest
```

如果没必要，不要引入大型 framework。

CLI 可使用：

```text
argparse
```

无需 Typer/Click，除非已有依赖。

---

# 22. 代码质量要求

> 状态：**已完成**。在原文文件列表之外增加 `layout_contract.py`（尺寸/锚定/流向投影）。无 2000 行 mega-file，无 plugin 架构。

这不是一次性脚本。

但也不要企业级过度设计。

希望结构大致：

```text
vellum_wpf_bridge/
├── __init__.py
├── cli.py
├── vellum_adapter.py
├── models.py
├── semantic_mapper.py
├── wpf_generator.py
├── resources.py
├── report.py
└── utils.py
```

如果实际实现有更简单方案，可以调整。

关键要求：

* 类型清楚
* 函数职责清楚
* 不做无意义 abstraction
* 不出现一个 2000 行 mega-file
* 不做 premature plugin architecture
* 不为理论上的未来需求写大量空代码

---

# 23. 不要做的事情

> 状态：**仍有效**。下列禁令继续遵守。

本阶段明确不要：

* 重写 Vellum
* 开发新的设计编辑器
* 接 OpenAI API
* 接本地 LLM
* 做 Figma importer
* 做完整 React generator
* 做完整 Avalonia generator
* 生成完整 ViewModel
* 做 pixel-perfect layout solver
* 做 OCR
* 做 screenshot-to-code
* 做复杂 GUI
* 做 VS Code extension

---

# 24. 成功标准

> 状态：**已完成**（2026-09-06 核对）。

| # | 标准 | 结果 |
| --- | --- | --- |
| 1 | ui-spec 人类可读 | 已完成 |
| 2 | Agent 好改 | 已完成 |
| 3 | XAML XML 合法 | 已完成 |
| 4 | 不是全 Canvas | 已完成 |
| 5 | Auto Layout → Stack/Grid | 已完成（有 fill 必须 Grid） |
| 6 | 左中右固定+自适应 | 已完成（1280→1600 canary：delta 0/+320/0） |
| 7 | 文本/色/圆角/padding | 已完成 |
| 8 | 不支持的不崩 | 已完成 |
| 9 | report 标明降级 | 已完成 |
| 10 | `dotnet build` | 已完成，0 错误 0 警告 |

完成后，我至少应该能够：

```bash
python -m vellum_wpf_bridge convert demo.vellum
```

得到：

```text
ui-spec.json
MainWindow.xaml
Resources.xaml
conversion-report.json
```

而且：

1. `ui-spec.json` 人类可读。
2. Agent 很容易理解和修改。
3. XAML XML 合法。
4. 基本布局不是全部 Canvas absolute positioning。
5. 自动布局能正确转换成 StackPanel/Grid。
6. 左/中/右布局可以正确表现固定 + 自适应宽度。
7. 文本、颜色、圆角、padding 等主要视觉属性得到保留。
8. unsupported feature 不会让转换器崩溃。
9. report 能明确指出降级内容。
10. 如果环境允许，生成的 WPF Demo 能 `dotnet build`。

---

# 25. 最后进行一次真实验证

> 状态：**已完成**。parser → mapper → generator → `python tests/run_tests.py`（31 passed）→ `dotnet build`。另有 WPF `ActualWidth` 运行时 canary。

不要只写代码就结束。

请实际执行：

```text
parser
↓
semantic mapper
↓
generator
↓
tests
```

至少跑一遍 demo。

然后检查生成的：

```text
ui-spec.json
MainWindow.xaml
```

人工快速审视：

* hierarchy 是否合理
* 有没有明显错误 absolute positioning
* 是否生成大量无意义 Grid
* 是否产生重复 Brush
* 是否保留关键布局结构
* XAML 是否可读

发现明显问题直接修复。

---

# 26. 最终汇报格式

> 状态：**已完成**（一次性交付指令，不再作为待办）。

完成后请用以下格式汇报：

## 完成内容

列出真正已经实现的功能。

## 项目结构

展示关键目录。

## Demo

说明使用了什么 `.vellum` sample。

## 实际执行命令

给出我可以直接复制的命令。

## 生成结果

展示：

```text
Vellum
→ UI Spec
→ XAML
```

中的一个实际小片段。

## 测试结果

明确写：

```text
X passed
Y failed
```

如果执行了：

```bash
dotnet build
```

写出结果。

## 当前限制

只列真实存在、影响下一步开发的限制。

不要写大量泛泛的“未来可以优化”。

## 下一步最值得做的 3 件事

按照价值排序。

---

# 27. 一个额外目标：把它设计成适合 Vibe Coding 的工具

> 状态：**仍有效**。现行补充：先建立可信的 Layout Contract，再允许 Semantic/Agent 改写它。尺寸、锚定和流向不能互相猜。

始终记住这个项目最终不是为了做传统 compiler。

它是为了形成这样的工作流：

```text
我：
“把右侧设置区缩窄，把模型选择改成三个 segmented buttons。”

Agent：
修改 UI Spec / Vellum design

我：
查看设计

我：
“可以，实现它。”

Agent：
生成 / 修改 WPF XAML
```

因此：

> Human readability 和 Agent editability
> 比理论上的 schema 完整性更重要。

如果在“完美表达 Vellum 所有图形属性”和“生成简单、稳定、Agent 容易修改的 UI Spec”之间冲突：

优先后者。

---

> 状态：**已完成**。一次性开工指令，不再执行。
>
> 现行工作目录：`tools/vellum-wpf-bridge/`。后续改动以文首「现行 Layout Contract」和 §23 / §27 为准，不要复活已废弃的 StackPanel 无条件映射或名字猜测 stretch。

现在开始检查代码库并直接实现这个 MVP。

不要只给设计方案。

实际创建文件、运行测试、生成 Demo、验证结果。

遇到非关键问题自行做合理决策继续，不要因为次要细节停止实现。
